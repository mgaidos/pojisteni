'use strict'

class Pojistenec {
    constructor(name, secondName, email, phone, street, city, postalCode, id) {
    
                this.name = name,
                this.secondName = secondName,
                this.email = email,
                this.phone = phone,
                this.street = street,
                this.city = city,
                this.postalCode = postalCode
                this.id = id
                this.dataOfInsurance = []

               
    }





}